/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Config file for defining DSL2 per module options and publishing paths
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Available keys to override module options:
        ext.args   = Additional arguments appended to command in module.
        ext.args2  = Second set of arguments appended to command in module (multi-tool modules).
        ext.args3  = Third set of arguments appended to command in module (multi-tool modules).
        ext.prefix = File name prefix for output files.
----------------------------------------------------------------------------------------
*/

process {

    publishDir = [
        path: { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" },
        mode: params.publish_dir_mode,
        saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
    ]

    withName: SAMPLESHEET_CHECK {
        publishDir = [
            path: { "${params.outdir}/pipeline_info" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: CUSTOM_DUMPSOFTWAREVERSIONS {
        publishDir = [
            path: { "${params.outdir}/pipeline_info" },
            mode: params.publish_dir_mode,
            pattern: '*_versions.yml'
        ]
    }
    withName: RENAME_INPUTS {
        publishDir = [
            path: { "${params.outdir}/rename_inputs" },
            enabled: false   ]
    }
    withName: RENAME_REFERENCE {
        publishDir = [
            path: { "${params.outdir}/rename_reference" },
            enabled: false ]
    }
    withName: SNIPPY_RUN {
        publishDir = [
            path: { "${params.outdir}/${species}/${cluster_id}/snippy_run" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: SNIPPY_CORE {
        publishDir = [
            path: { "${params.outdir}/${species}/${cluster_id}/snippy_core" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: SNPDISTS {
        publishDir = [
            path: { "${params.outdir}/${species}/${cluster_id}/snippy_core" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: IQTREE {
        ext.args = "-redo"
        publishDir = [
            path: { "${params.outdir}/${species}/${cluster_id}/snippy_core" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: CLEAN_TREE {
        publishDir = [
            path: { "${params.outdir}/${species}/${cluster_id}/snippy_core" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: GUBBINS {
        ext.args = '--verbose'
        publishDir = [
            path: { "${params.outdir}/${species}/${cluster_id}/gubbins" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: SNPSITES {
        publishDir = [
            path: { "${params.outdir}/${species}/${cluster_id}/gubbins" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: SNPDISTS_GUBBINS {
        publishDir = [
            path: { "${params.outdir}/${species}/${cluster_id}/gubbins" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: PANAROO_RUN {
        publishDir = [
            path: { "${params.outdir}/${species}/${cluster_id}" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: GENEDISTS {
        publishDir = [
            path: { "${params.outdir}/${species}/${cluster_id}/panaroo" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: MASHTREE {
        publishDir = [
            path: { "${params.outdir}/${species}/${cluster_id}/mashtree" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: REFERENCE_EVALUATION {
        publishDir = [
            path: { "${params.outdir}/${species}/${cluster_id}/" },
            mode: params.publish_dir_mode ,
        ]
    }
    withName: YAML_REPORT {
        publishDir = [
            path: { "${params.outdir}/${species}/${cluster_id}/" },
            enabled: false
        ]
    }
    withName: QUARTO_REPORT {
        publishDir = [
            path: { "${params.outdir}/${species}/${cluster_id}/" },
            mode: params.publish_dir_mode ,
            pattern: "*.html"
        ]
    }
}
